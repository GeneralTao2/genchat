<% if (items.length) { %>
  <ul>
    <% } %>

      <% items.forEach(function(item){ %>
        <li id="<%= item.id %>">
          <div class="acomment">
            <div class="heads">
              <div class="head">
                <a href="/users/<%= item.owner.login %>">
                  <%= item.owner.login %>
                </a>
                <spam class="date">
                  <%= moment(item.createdAt).fromNow(); %>
                </spam>
              </div>
                <span align="right" class="link reply" style="display: none;">ответить</span>
            </div>
            <%= item.body %>
            </div>
              <%- include('./comments.ejs', {items: item.children}) %>
        </li>
        <% }) %>

          <% if (items.length) { %>
  </ul>
  <% } %>
